{
  "_comment": "REFERENCE ONLY — this file is not read by any runtime code. LLM models are configured via .env role-specific vars. Paths and safety settings are hardcoded in harness modules. Persona paths are superseded by prompts/.",
  "project": "pm_build_mvp",
  "version": "0.1.0",
  "llm": {
    "provider": "openai",
    "model": "gpt-4o",
    "temperature": 0.3,
    "max_tokens": 4096
  },
  "workspace": {
    "root": "./workspace",
    "current_dir": "./workspace/current",
    "archive_dir": "./workspace/archive",
    "raw_ideas_file": "./workspace/raw_ideas.md"
  },
  "logs": {
    "dir": "./logs",
    "level": "INFO"
  },
  "safety": {
    "max_file_size_kb": 512,
    "audit_enabled": true,
    "schema_validation": true
  },
  "personas": {
    "_comment": "reference only — personas/ files are not runtime-loaded; active prompts live in prompts/",
    "pm_director": "./personas/pm_director.md",
    "product_pm": "./personas/product_pm.md",
    "qa_pm": "./personas/qa_pm.md"
  }
}
